<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.maint.dict.mapper.IDbSupplierMapper">
	<!-- 查询语句 -->
	<select id="findAllcombox" resultType="DbSupplier">
		SELECT * FROM db_supplier d where d.statisticsId=#{statisticsId}
	</select>

</mapper>